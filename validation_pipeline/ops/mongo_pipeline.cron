# mongo_pipeline 패키지 디렉터리와 가상환경 경로를 운영 환경에 맞게 수정한다.
# 매 3분마다 한 tick만 실행하므로 이전 프로세스와 겹치면 lock이 건너뛴다.
*/3 * * * * cd /opt/mongo-pipeline/gpt && /opt/mongo-pipeline/.venv/bin/python -m mongo_pipeline --config config.json --scheduled-once >> logs/cron.log 2>&1
