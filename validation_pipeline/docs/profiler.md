# 프로파일 (`profiler.py`)

## 역할

실제 데이터의 최상위 필드와 타입을 실행 리포트에 요약합니다.

- 필드별 등장 횟수
- 필드별 누락 횟수와 누락률
- 필드별 실제 타입 분포

구조 파악을 위해 표준화에는 성공했지만 검증에서 제외된 문서도 포함합니다.

## 수정 지점

중첩 경로나 값 통계가 필요할 때만 `SchemaProfiler.observe()`와 `report()`를 확장합니다.
